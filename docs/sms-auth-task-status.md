# SMS・メール 2段階認証自動化タスク進捗状況

複数エージェントで協調して開発を行うための進捗管理ドキュメントです。

## タスク一覧

### フェーズ1：事前準備（現行システム稼働中）
- [x] Flask API (`server.py`) への新規エンドポイント実装
  - [x] `POST /api/sms-code` (認証コード受信)
  - [x] `GET /api/sms-code` (認証コード取得)
  - [x] `DELETE /api/sms-code` (認証コードクリア)
- [x] APIエンドポイントの単体テストの追加・実施 (`tests/` 配下に新規テストスクリプト)
- [x] 独立検証用スクリプトの作成 (`scratch/test_cookie_auth.py` 等)
  - [x] 現行ログイン画面でのCookieエクスポート機能の実装とテスト
  - [x] 保存したCookieインポートによるセッション復元＆ログイン省略テスト
  - [x] Chromeプロファイル固定化 (`--user-data-dir`) の動作テスト
- [x] エージェント代行：現行システムでの検証テスト実行（結果：現行システムはCookieを一切使用せず、HTMLフォーム内のhiddenパラメータ `SessionID` に依存する仕様であることを確認。これによりCookie/プロファイルによるセッション復元は現行仕様上不可と判明）
- [ ] ~~ユーザー側: スマートフォン (Tasker等) の転送設定とAPI疎通テスト~~ **【不採用 / 不要】**
- [ ] （Gmail API利用）Google Cloud Consoleでのプロジェクト作成および `credentials.json` の作成・検証

### フェーズ2：刷新直後（8月予定）の初期調査
- [ ] 新ログイン画面のHTML構造・Selector・URL遷移仕様の調査
- [ ] 新マイページおよび新車両情報画面等のHTML構造・Selector・URL遷移仕様の調査
- [ ] 手動/ノンヘッドレスでの検証
  - [ ] 同一プロファイル/Cookie維持による都度ログインでのSMS回避可否の調査
  - [ ] エリア巡回時のログアウト〜ログインにおけるSMS発生挙動の調査
  - [ ] WEBサイトの構成変更がスクレイピング（データの表構造等）に与える影響の評価

### フェーズ3：自動化実装・統合テスト
- [ ] `src/auth.py` への新ログイン・2段階認証ポーリング処理の実装
- [ ] （Gmail API利用）`src/gmail_client.py` の新規作成およびポーリング監視ロジックの実装
- [ ] `src/browser.py` へのプロファイル永続化の組み込み（Cookieベースセッション移行時用）
- [ ] `src/scraper.py` 等への新WEBサイト構成に対応したデータ抽出・解析処理の修正
- [ ] 実機・模擬メールを用いた結合テストの実施
- [ ] タイムアウト値、ポーリング周期のチューニング
